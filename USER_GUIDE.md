# 📖 คู่มือการใช้งาน - บัญชีรายจ่ายสวน

## 🎯 เริ่มต้นใช้งาน

### ขั้นตอนที่ 1: ติดตั้ง XAMPP (MySQL)

1. **ดาวน์โหลด XAMPP**
   - ไปที่: https://www.apachefriends.org/
   - ดาวน์โหลดเวอร์ชันล่าสุด (PHP 8.x)
   - ติดตั้งตามปกติ

2. **เปิด XAMPP Control Panel**
   - เปิดโปรแกรม XAMPP
   - คลิก **Start** ที่ Apache
   - คลิก **Start** ที่ MySQL
   - ทั้งสองต้องเป็นสีเขียว

3. **สร้างฐานข้อมูลผ่าน phpMyAdmin**
   - เปิด Browser ไปที่: http://localhost/phpmyadmin
   - คลิก Tab **"Import"**
   - คลิก **"Choose File"**
   - เลือกไฟล์ `backend/database.sql`
   - คลิก **"Go"** ด้านล่าง
   - ✅ เสร็จแล้ว! ฐานข้อมูล `garden_expense` ถูกสร้างพร้อมข้อมูลตัวอย่าง

### ขั้นตอนที่ 2: ติดตั้ง Node.js

1. **ดาวน์โหลด Node.js**
   - ไปที่: https://nodejs.org/
   - ดาวน์โหลด LTS version (แนะนำ)
   - ติดตั้งตามปกติ

2. **ตรวจสอบการติดตั้ง**
   ```bash
   node --version
   npm --version
   ```
   ควรเห็นเลขเวอร์ชัน (เช่น v18.17.0)

### ขั้นตอนที่ 3: ติดตั้งโปรเจค

1. **เปิด Terminal/Command Prompt**
   - Windows: กด `Win + R` → พิมพ์ `cmd` → Enter
   - Mac: กด `Cmd + Space` → พิมพ์ `terminal` → Enter

2. **ไปที่โฟลเดอร์โปรเจค**
   ```bash
   cd ที่อยู่/โฟลเดอร์/garden-expense-tracker
   ```

3. **ติดตั้ง Backend**
   ```bash
   cd backend
   npm install
   ```
   รอจนเสร็จ (ประมาณ 1-2 นาที)

4. **ติดตั้ง Frontend**
   ```bash
   cd ../frontend
   npm install
   ```
   รอจนเสร็จ (ประมาณ 1-2 นาที)

### ขั้นตอนที่ 4: รันระบบ

**วิธีที่ 1: ใช้สคริปต์ (แนะนำ)**

Windows:
```bash
# Double-click ไฟล์ start.bat
start.bat
```

Mac/Linux:
```bash
# ใน Terminal
./start.sh
```

**วิธีที่ 2: รันแบบแยกส่วน**

Terminal 1 (Backend):
```bash
cd backend
npm start
```

Terminal 2 (Frontend):
```bash
cd frontend
npm run dev
```

### ขั้นตอนที่ 5: เปิดใช้งาน

เปิด Browser ไปที่: **http://localhost:3000**

✅ เสร็จแล้ว! พร้อมใช้งาน

---

## 🎨 การใช้งานระบบ

### 1. หน้าแรก (Dashboard)

**บัตรสรุปยอด:**
- แสดงยอดรายจ่ายทั้งหมด
- แสดงช่วงวันที่ที่เลือก

**ปุ่มด้านบน:**
- **Export Excel**: ดาวน์โหลดข้อมูลเป็น CSV
- **ส่งสรุปไปไลน์**: ส่งสรุปรายจ่ายวันนี้ไปไลน์ (ต้องตั้งค่า LINE Notify ก่อน)
- **เพิ่มรายจ่าย**: เพิ่มรายการใหม่

### 2. เพิ่มรายจ่าย

1. คลิกปุ่ม **"เพิ่มรายจ่าย"** (สีแดง)
2. กรอกข้อมูล:
   - **หมวดหมู่**: เลือกจาก 10 หมวด
   - **จำนวนเงิน**: ใส่ตัวเลข (เช่น 800)
   - **รายละเอียด**: ระบุรายละเอียด (ไม่บังคับ)
   - **วันที่**: เลือกวันที่
3. คลิก **"บันทึก"**

**หมวดหมู่ที่มี (10 หมวด):**
- ปุ๋ย
- เมล็ดพันธุ์
- น้ำ
- ค่าแรง
- อุปกรณ์
- ยากำจัดศัตรูพืช
- ค่าขนส่ง
- ค่าไฟฟ้า
- ซ่อมบำรุง
- รายจ่ายอื่นๆ

### 3. แก้ไขรายการ

1. คลิกปุ่ม **"แก้ไข"** (สีฟ้า) ที่รายการที่ต้องการ
2. แก้ไขข้อมูล
3. คลิก **"บันทึก"**

### 4. ลบรายการ

1. คลิกปุ่ม **"ลบ"** (สีแดง) ที่รายการที่ต้องการ
2. ยืนยันการลบ

### 5. กรองข้อมูล

**กรองตามช่วงวันที่:**
1. เลือก **"วันที่เริ่มต้น"**
2. เลือก **"วันที่สิ้นสุด"**
3. ระบบจะแสดงข้อมูลในช่วงนั้นอัตโนมัติ

### 6. Export Excel

1. กรองวันที่ที่ต้องการ (ถ้าจำเป็น)
2. คลิกปุ่ม **"Export Excel"** (สีเขียว)
3. ไฟล์ CSV จะดาวน์โหลด (ชื่อ: expenses-YYYY-MM-DD.csv)
4. เปิดด้วย Microsoft Excel หรือ Google Sheets

**เปิดไฟล์ Excel:**
- Double-click เปิดใน Excel
- หรือ Import ใน Google Sheets
- ข้อมูลจะแสดงเป็นภาษาไทย (UTF-8)

### 7. ดูกราฟวิเคราะห์

1. คลิก Tab **"กราฟวิเคราะห์"**
2. ดูกราฟ 3 แบบ:
   - **กราฟวงกลม**: สัดส่วนรายจ่ายตามหมวดหมู่
   - **กราฟแท่ง**: เปรียบเทียบรายจ่ายแต่ละหมวดหมู่
   - **กราฟเส้น**: แนวโน้มรายจ่ายรายเดือน

---

## 💬 การตั้งค่า LINE Notify (ไม่บังคับ)

### เพื่ออะไร?
- รับแจ้งเตือนเมื่อเพิ่มรายจ่าย
- กดปุ่มส่งสรุปรายจ่ายไปไลน์
- รับสรุปอัตโนมัติทุกวัน 20:00 น.

### วิธีตั้งค่า (5 นาที)

1. **สร้าง LINE Notify Token**
   - เปิด: https://notify-bot.line.me/
   - กด "เข้าสู่ระบบ" (Login)
   - ล็อกอินด้วยไลน์
   - กด "My page" (มุมบนขวา)
   - กด "Generate token"
   - ตั้งชื่อ: "บัญชีรายจ่ายสวน"
   - เลือก: "1-on-1 chat with LINE Notify"
   - กด "Generate token"
   - **คัดลอก Token** (จะแสดงครั้งเดียว!)

2. **ใส่ Token ในระบบ**
   - เปิดไฟล์ `backend/.env`
   - หาบรรทัด `LINE_NOTIFY_TOKEN=`
   - วาง Token ที่คัดลอกไว้
   - บันทึกไฟล์

3. **Restart Backend**
   ```bash
   # กด Ctrl+C ใน Terminal ที่รัน Backend
   # แล้วรันใหม่
   npm start
   ```

4. **ทดสอบ**
   - เพิ่มรายจ่ายในเว็บ
   - เช็คไลน์ → ต้องได้รับข้อความแจ้งเตือน!

**ข้อความที่จะได้รับ:**
```
🌱 รายจ่ายใหม่
━━━━━━━━━━━━━━━━
📅 วันที่: 7 ธันวาคม 2567
📂 หมวดหมู่: ปุ๋ย
💰 จำนวน: 800.00 บาท
📝 รายละเอียด: ซื้อปุ๋ยยูเรีย
━━━━━━━━━━━━━━━━
```

---

## 🗄️ การจัดการฐานข้อมูล

### ดูข้อมูลใน phpMyAdmin

1. เปิด: http://localhost/phpmyadmin
2. คลิกฐานข้อมูล `garden_expense`
3. ดูตาราง:
   - **categories**: หมวดหมู่
   - **transactions**: รายการรายจ่าย

### Views (สำหรับวิเคราะห์)

**monthly_expenses**: สรุปรายเดือน
```sql
SELECT * FROM monthly_expenses;
```

**category_expenses**: สรุปตามหมวดหมู่
```sql
SELECT * FROM category_expenses;
```

**recent_expenses**: รายการล่าสุด
```sql
SELECT * FROM recent_expenses;
```

**today_expenses**: รายจ่ายวันนี้
```sql
SELECT * FROM today_expenses;
```

### Stored Procedures

**ดูสรุปช่วงเวลา:**
```sql
CALL GetExpenseSummary('2024-12-01', '2024-12-31');
```

**ดู Top 10 รายจ่ายสูงสุด:**
```sql
CALL GetTopExpenses(10);
```

**เพิ่มรายจ่ายใหม่:**
```sql
CALL AddExpense(1, 800.00, 'ซื้อปุ๋ย', CURDATE());
```

### Backup ข้อมูล

**ผ่าน phpMyAdmin:**
1. เลือกฐานข้อมูล `garden_expense`
2. คลิก Tab "Export"
3. เลือก "Quick"
4. คลิก "Go"
5. ไฟล์ .sql จะดาวน์โหลด

**ผ่าน Command Line:**
```bash
# Windows (XAMPP)
C:\xampp\mysql\bin\mysqldump -u root garden_expense > backup.sql

# Mac/Linux
mysqldump -u root -p garden_expense > backup.sql
```

### Restore ข้อมูล

**ผ่าน phpMyAdmin:**
1. เลือกฐานข้อมูล `garden_expense`
2. คลิก Tab "Import"
3. เลือกไฟล์ backup.sql
4. คลิก "Go"

---

## ❓ แก้ไขปัญหา

### ❌ Backend ไม่ทำงาน

**Error: Cannot find module**
```bash
cd backend
npm install
```

**Error: ECONNREFUSED 127.0.0.1:3306**
- เปิด XAMPP
- Start MySQL
- ตรวจสอบว่า MySQL ทำงานอยู่

**Error: Access denied for user 'root'**
- เปิดไฟล์ `backend/.env`
- ตรวจสอบ `DB_PASSWORD`
- ถ้ามีรหัสผ่าน ให้ใส่ไว้

### ❌ Frontend ไม่ทำงาน

**Error: Cannot find module**
```bash
cd frontend
npm install
```

**หน้าเว็บว่างเปล่า**
- เช็คว่า Backend ทำงานอยู่
- เปิด Console (F12) ดู error
- Refresh หน้า (Ctrl+F5)

### ❌ ไม่สามารถเพิ่มรายจ่ายได้

1. เช็ค Console (F12)
2. ดู Network tab
3. ตรวจสอบว่า API ตอบกลับหรือไม่

### ❌ LINE Notify ไม่ทำงาน

1. ตรวจสอบ `LINE_NOTIFY_TOKEN` ใน `.env`
2. Token ต้องไม่มีเว้นวรรค
3. ต้อง Restart Backend หลังแก้ `.env`
4. เช็ค Backend Console ดูว่ามี error

### ❌ Export Excel ไม่ได้

1. ตรวจสอบว่า Backend ทำงานอยู่
2. ลองเปิด URL โดยตรง:
   ```
   http://localhost:3001/api/export/csv
   ```
3. ถ้าไม่ได้ แสดงว่า Backend มีปัญหา

### ❌ ภาษาไทยใน Excel แสดงผิด

1. ไฟล์ CSV มี UTF-8 BOM อยู่แล้ว
2. เปิดด้วย Excel โดยตรง (Double-click)
3. ถ้ายังผิด ลองใช้ Google Sheets

---

## 💡 เคล็ดลับการใช้งาน

### 1. Keyboard Shortcuts

- `Ctrl + F5`: Refresh หน้า (Clear cache)
- `F12`: เปิด Developer Tools
- `Ctrl + C`: หยุด Server ใน Terminal

### 2. ประหยัดเวลา

- ตั้งค่าช่วงวันที่เป็น "เดือนนี้" ไว้
- ใช้ Tab กดเปลี่ยนช่อง แทนใช้เมาส์
- เพิ่มรายจ่ายทุกวันก่อนนอน

### 3. การสำรอง

- Backup ฐานข้อมูลทุกสัปดาห์
- Export Excel เก็บไว้ทุกเดือน
- เก็บไฟล์ backup ไว้ 3 ที่

### 4. ความปลอดภัย

- อย่าแชร์ LINE_NOTIFY_TOKEN
- ตั้งรหัสผ่าน MySQL
- Backup ข้อมูลสม่ำเสมอ

---

## 📞 ติดต่อ & ช่วยเหลือ

### เอกสารเพิ่มเติม

- **MYSQL_SETUP.md**: คู่มือติดตั้ง MySQL/phpMyAdmin
- **LINE_QUICKSTART.md**: เริ่มใช้งาน LINE Notify
- **LINE_INTEGRATION.md**: คู่มือ LINE ละเอียด
- **N8N_INTEGRATION.md**: เชื่อมกับ n8n
- **PRODUCTION_ROADMAP.md**: แผนพัฒนาเป็นแอปจริงจัง

### ปัญหาที่พบบ่อย

ดูเพิ่มเติมที่ส่วน **แก้ไขปัญหา** ด้านบน

---

**Happy Tracking! 🌱**
